# FastAPI and core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP requests
requests==2.31.0

# Vector database and AI
chromadb==0.5.23
google-generativeai==0.3.2

# CORS middleware (included in fastapi but being explicit)
# python-multipart==0.0.6  # uncomment if we add file uploads

# For future AI integration
# openai==1.3.0  # uncomment when ready
# anthropic==0.7.0  # alternative to openai

# Dev dependencies (install with: pip install -r requirements.txt -r requirements-dev.txt)
# pytest==7.4.3
# pytest-asyncio==0.21.1
# httpx==0.25.2  # for testing FastAPI endpoints
